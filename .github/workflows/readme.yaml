name: Workflow

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - feature/**
jobs:
   run:
     runs-on: ubuntu-latest

     steps:
        - name: Checkout
          uses: actions/checkout@v3
